/**
 * 
 * @param {Object} options
 * @param {String} options.name name of question
 * @param {Number} options.limit number question will be geted 
 */
export const apiQuestion = ({
  name,
  limit
}) => {

}

apiQuestion({
  limit,
  name
})