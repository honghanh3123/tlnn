import { StyleSheet } from 'react-native'

export const baseLayout = StyleSheet.create({
  wrapper: {
    flex: 1
  }
})

export const baseText = StyleSheet.create({
  title: {
    textAlign: "center"
  }
})

export const baseInput = StyleSheet.create({
  fullWidth: {
    width: "100%"
  }
})